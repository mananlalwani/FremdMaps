# No server-side environment variables required.
# All data is served as static files from client/public/data/.

# Set to 'true' to allow ?dev outside development builds.
# Default (unset/false): developer tools are disabled in production deployments.
PUBLIC_ENABLE_DEV=false
