# No server-side environment variables required.
# All data is served as static files from client/public/data/.

# Set to 'true' to allow ?dev on non-local production deployments.
# Default (unset/false): developer tools are disabled there; localhost is always enabled.
PUBLIC_ENABLE_DEV=false
