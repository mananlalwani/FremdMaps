# No server-side environment variables required.
# All data is served as static files from client/public/data/.

# Set to 'true' to allow ?admin outside development builds.
# Default (unset/false): admin mode is disabled in production deployments.
PUBLIC_ENABLE_ADMIN=false
